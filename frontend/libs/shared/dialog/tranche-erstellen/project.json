{
  "name": "shared-dialog-tranche-erstellen",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/dialog/tranche-erstellen/src",
  "prefix": "dv",
  "projectType": "library",
  "tags": ["type:dialog", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:noop",
      "options": {
        "tsConfig": "libs/shared/dialog/tranche-erstellen/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/shared/dialog/tranche-erstellen/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
