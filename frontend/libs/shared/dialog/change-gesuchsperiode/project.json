{
  "name": "shared-dialog-change-gesuchsperiode",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/dialog/change-gesuchsperiode/src",
  "prefix": "dv",
  "projectType": "library",
  "tags": ["type:dialog", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:noop",
      "options": {
        "tsConfig": "libs/shared/dialog/change-gesuchsperiode/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/shared/dialog/change-gesuchsperiode/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
