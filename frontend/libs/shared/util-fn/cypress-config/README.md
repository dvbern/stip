# shared-util-fn-cypress-config

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test shared-util-fn-cypress-config` to execute the unit tests via [Jest](https://jestjs.io).
