apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: www
  annotations:
    route.openshift.io/termination: "edge"
spec:
  rules:
    - host: "<overlay-specific>"
      http:
        paths:
          - path: "/"
            backend:
              service:
                name: gesuch-app
                port:
                  name: http
                  number: 80
            pathType: Prefix
  tls:
    - {}